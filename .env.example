# Operon v2 — Ollama configuration
# No API keys needed! Ollama runs locally.

# Ollama model to use (must be pulled first — docker-compose does this automatically)
# Options: llama3.2, llama3.1, mistral, codellama, deepseek-coder, qwen2.5-coder
OLLAMA_MODEL=llama3.2
